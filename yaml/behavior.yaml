modules
  name: Behavior

  description: |
    <module description>
  
properties
  view: |
    @type {<type>}
    
  defaults: |
    @type {<type>}
    
  options: |
    @type {<type>}
    
  $: |
    @type {<type>}
    
  $el: |
    @type {<type>}
    
  el: |
    @type {<type>}
    
functions
  initialize:
    description: | 
      @api public
  
    examples:
      -
        name:
        example: |
      
  destroy:
    description: | 
      @api public
  
    examples:
      -
        name:
        example: |
      
  proxyViewProperties:
    description: | 
      @api public
      @param {<type>} view - 
  
    examples:
      -
        name:
        example: |
      
